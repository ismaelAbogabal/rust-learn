use std::collections::HashMap;

pub(crate) fn test() {}

pub fn game_of_life(board: &mut Vec<Vec<i32>>) {
    let born = vec![] as Vec<usize>;
    let death = vec![] as Vec<usize>;
}

pub fn neighbour(board: &Vec<Vec<i32>>, r: usize, c: usize) {
    // we could be graph and jump the graph to get the neighbour

    // we could travel the matrix to see neighbours with another array
}
